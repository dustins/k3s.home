apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmCharts:
- name: metallb
  includeCRDs: true
  releaseName: metallb
  version: 0.14.8
  repo: https://metallb.github.io/metallb
  valuesFile: values.yaml

resources:
- metallb.ipaddress-pool.k8s-home.yaml
